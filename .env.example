# Flask Secret Key (change this to a random string)
SECRET_KEY="a_very_secret_key"

# Fernet encryption key for securing credentials.
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=""

# Unsplash API Key (optional, for image fetching)
UNSPLASH_ACCESS_KEY=""

# Note: Twitter API keys are now stored per-user in the database,
# so they are no longer needed here.
